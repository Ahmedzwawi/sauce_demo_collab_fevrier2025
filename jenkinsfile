pipeline {
    agent {
        docker {
            image "cypress/included"
            args '--entrypoint=""'  
        }
    }


    stages {
        stage('Vérifier la version de Cypress') {
            steps {
                sh "cypress --version"
            }
        }

        stage('Exécuter les tests API avec Cypress') {
            steps {
                sh 'npx cypress run'
            }
        }
    }

}

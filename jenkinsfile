pipeline {
    agent {
        docker {
            image "cypress/browsers"
            args '--entrypoint=""'  
        }
    }


    stages {
        stage('Vérifier la version de Cypress') {
            steps {
                sh "npm --version"
            }
        }

        stage('Exécuter les tests API avec Cypress') {
            steps {
                sh 'npx cypress run'
            }
        }
    }

}
